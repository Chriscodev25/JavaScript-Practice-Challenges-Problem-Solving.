// fizzBuzz
function fizzBuzz(str1, str2) {
    let combinedLength = str1.length + str2.length;
    if (combinedLength % 3 === 0) {
        return "Fizz";
    } else if (combinedLength % 5 === 0) {
        return "Buzz";
    } else if (combinedLength ==)
}
